import {StyleSheet} from 'react-native'

import {display} from '@utils'

export const CVCInputStyles = () =>
  StyleSheet.create({
    infoIcon: {
      position: 'absolute',
      top: display.px(-15),
      left: display.px(60),
    },
  })
